# Python dependencies for the typhoid prediction pipeline.
# Versions pinned to match the website's `requirements.txt` so the
# pipeline reproduces the same numbers visitors see on the rendered site.
#
# Install: pip install -r code/requirements.txt
# Run:     python code/main.py             (full 4-model + ensemble)
#          python code/paper_main.py       (3-model + paper figures)

pandas==2.2.3
numpy==2.2.0
scikit-learn==1.6.1
xgboost==2.1.3
matplotlib==3.10.0
seaborn==0.13.2

# Optional — only needed if you want the true LSTM (TensorFlow) rather
# than the MLP fallback. Comment out on machines without GPU/CUDA.
# tensorflow

# Notebook / kernel — only needed if you'll re-render the Quarto site.
jupyter==1.1.1
nbformat==5.10.4
ipykernel==6.30.1
