# Python dependencies for the Quarto site.
# Pin versions so the CI build and local build produce identical results.
#
# Update with: pip install -U <pkg> then `pip freeze | grep <pkg>` to capture
# the new pin. Don't run a blind `pip freeze` — it adds transitive pins that
# break on other platforms.

jupyter==1.1.1
nbformat==5.10.4
ipykernel==6.30.1
numpy==2.2.0
pandas==2.2.3
scikit-learn==1.6.1
matplotlib==3.10.0
xgboost==2.1.3
seaborn==0.13.2
